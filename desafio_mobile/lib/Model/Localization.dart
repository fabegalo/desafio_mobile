class Localization {
  int? id;
  String? latitude;
  int? longitude;
  String? datetime;

  Localization({this.id, this.latitude, this.longitude, this.datetime});

  // factory Localization.fromJson(Map<String, dynamic> json) {
  //   return Login(id: null, latitude: null, longitude: null, datetime: null);
  // }
}
